import 'package:flutter/widgets.dart';

class KomkatFilledButton extends StatelessWidget {
  const KomkatFilledButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
